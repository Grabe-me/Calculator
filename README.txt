Для запуска проекта необходим предустановленный Docker compose

Сборка проекта производится локально (необходимо скачать полный пакет файлов приложения)

Запуск командой: docker-compose up -d

Сервис доступен по адресу: localhost:8000
_________________________________________________________________________________________

You should have pre-installed Docker compose

It's local-build application (it's nesessary to download all app's files)

To start app use command: docker-compose up -d

Application is available on: localhost:8000